global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'alb'
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets: [ "${ALB_DNS}" ]
